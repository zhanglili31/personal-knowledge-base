# 个人知识库初始化

> 这是一次性初始化任务书。请把本文件交给 AI，并告诉 AI：**“按照这份文档帮我完成初始化。能自动完成的全部直接完成，只有必须我操作时再一步一步告诉我。”**
>
> 初始化全部完成后，AI 必须把本文件移动到 `archive/system/初始化-YYYY-MM-DD.md`，补充实际执行结果并提交 Git。不要让用户手动整理本文件。

---

## 0. AI 的执行原则

本知识库面向 **0 基础用户**。

用户负责：

- 说清楚自己想做什么。
- 在登录、授权、系统弹窗等 AI 无法代替操作时按提示点击。
- 对删除、大规模覆盖、冲突等高风险动作做最终决定。

AI 负责：

- 检查环境。
- 安装和配置工具。
- 执行终端命令。
- 配置 Git / GitHub。
- 安装知识库需要的插件。
- 检查仓库状态。
- 修复常见错误。
- 提交并同步 Git。
- 最后验证整套环境是否可用。

**不要让用户复制执行一串命令。AI 有终端权限时应自己执行。**

如果某一步 AI 无法自动完成，只告诉用户当前必须做的一个动作，用户完成后继续，不要一次抛出一堆操作。

---

# 1. 使用工具：TRAE

本知识库默认使用 **TRAE** 作为日常操作入口。

AI 初始化时先确认：

1. TRAE 已安装并可以启动。
2. 用户已经用 TRAE 打开本知识库根目录。
3. TRAE 中的 AI Agent 能访问当前工作区和终端。

如果 TRAE 未安装，AI 应根据当前操作系统指导用户下载安装；安装完成后继续，不要求用户自己研究配置。

---

# 2. 检查 Git

AI 先执行：

```bash
git --version
```

如果 Git 已安装，继续下一步。

如果未安装：

- AI 先识别 Windows / macOS / Linux。
- 能通过系统包管理器安全安装时，由 AI 直接安装。
- 如果必须经过图形安装器，AI 给用户官方安装方式，并只指导必要选项。
- 安装完成后 AI 再次执行 `git --version` 验证。

不要要求 0 基础用户理解 Git 安装参数。

---

# 3. 配置 Git 身份

AI 执行：

```bash
git config --global user.name
git config --global user.email
```

如果已经有合理配置，不重复修改。

如果为空，AI 只询问用户缺失的信息，例如：

> Git 提交需要一个名字和邮箱。告诉我你希望显示的名字和 GitHub 邮箱，其余我来配置。

然后由 AI 执行：

```bash
git config --global user.name "用户名字"
git config --global user.email "用户邮箱"
```

再次读取配置验证。

---

# 4. 配置 GitHub 登录

AI 先检查当前仓库远程地址：

```bash
git remote -v
```

再检查 GitHub CLI：

```bash
gh --version
```

如果 `gh` 不存在，AI 根据当前系统自动安装 GitHub CLI；无法自动安装时才指导用户完成安装。

然后检查：

```bash
gh auth status
```

如果已经登录正确 GitHub 账号，不重复登录。

如果未登录，由 AI 发起：

```bash
gh auth login
```

此步骤如果出现浏览器授权、验证码或账号确认，AI 应明确告诉用户当前只需要做什么。授权完成后继续执行：

```bash
gh auth status
git remote -v
```

目标：用户以后不需要自己处理 GitHub Token、SSH Key 等细节，除非现有环境明确需要。

---

# 5. 安装 TRAE 插件

AI 检查以下插件是否已经安装。已经存在的不要重复安装。

## 必装

| 插件 ID | 用途 |
|---|---|
| `TAKASHI-UCHIDA.VSCODE-OFFICE-VIEWER` | 在编辑器中预览 Word、Excel、PowerPoint、PDF 等常见文档 |
| `MS-VSCODE.LIVE-SERVER` | 本地预览 HTML 等网页内容 |

### PDF 插件

`tomoki1207.PDF` **默认不再单独安装**。

原因：当前 `TAKASHI-UCHIDA.VSCODE-OFFICE-VIEWER` 已支持 `.pdf`，同时支持 `.docx`、`.xlsx`、`.xlsm`、`.pptx`、`.csv` 等格式。对普通用户而言再安装一个独立 PDF Viewer 属于重复功能。

只有在 Office Viewer 的 PDF 预览出现兼容问题时，AI 才考虑补装：

```text
tomoki1207.PDF
```

### TRAE 中的安装原则

AI 优先在 TRAE 扩展市场搜索插件 ID 并安装。

如果 TRAE 商店没有该插件：

1. AI 获取对应 VS Code Marketplace 的 `.vsix`。
2. 指导或自动让 TRAE 从 VSIX 安装。
3. 安装完成后验证插件已经启用。

用户不需要理解 VSIX 是什么。

---

# 6. 安装 MarkItDown

知识库需要让 AI 尽量理解 PDF、Word、PPT、Excel、HTML、图片等不同类型文件。

AI 检查：

```bash
markitdown --help
```

如果不可用：

1. 检查 Python 环境。
2. 优先创建知识库自己的本地 Python 虚拟环境，避免污染用户其他软件环境。
3. 安装 Microsoft 开源的 MarkItDown，并包含常用文件格式依赖。
4. 不把虚拟环境提交到 Git。

推荐安装目标：

```text
markitdown[all]
```

AI 应根据当前系统选择实际可用的 Python 命令，不要让用户判断 `python`、`python3`、`py` 的区别。

安装后至少测试一个转换命令是否可运行。

## 文件处理原则

MarkItDown 生成的 Markdown 是 **AI 阅读层**，不是原始文件替代品。

处理非 Markdown 文件时：

```text
原文件
  ↓ 保留
sources/ 或原有业务目录
  ↓
MarkItDown
  ↓
.knowledge/generated/
  ↓
AI 理解、摘要、分类、关联
```

AI 不得因为转换成功而删除原始文件。

---

# 7. 检查知识库目录

AI 确认至少存在：

```text
inbox/
projects/
areas/
knowledge/
records/
sources/
archive/
.knowledge/
.knowledge/generated/
```

缺少时由 AI 创建。

不要为了“看起来完整”提前创建大量细分类目录。真正有内容以后再自然生长。

---

# 8. 检查安全与 Git 忽略规则

AI 检查 `.gitignore`，至少避免提交：

```text
.env
.env.*
*.key
*.pem
.DS_Store
Thumbs.db
.knowledge/.venv/
__pycache__/
```

如果知识库是公开仓库，AI 必须提醒用户：个人知识、账号信息、合同、证件、隐私资料等不应进入公开 GitHub 仓库。

涉及敏感内容时，优先建议把仓库设为 Private。

---

# 9. Git 初始化与同步检查

AI 自动完成：

```bash
git status
git branch --show-current
git remote -v
```

然后：

1. 检查是否存在用户未提交修改。
2. 不覆盖用户修改。
3. 拉取远程更新时优先使用不会破坏本地工作的方式。
4. 处理初始化产生的文件。
5. 创建一个明确的初始化提交。
6. 推送到 GitHub。

推荐提交信息：

```text
chore: initialize personal knowledge environment
```

用户不需要自己执行 `add`、`commit`、`pull`、`push`。

---

# 10. 最终验收

AI 必须逐项验证，而不是安装完就宣称完成：

- [ ] TRAE 可以正常打开知识库
- [ ] Git 可用
- [ ] Git 用户名和邮箱已配置
- [ ] GitHub 已登录
- [ ] 当前仓库远程地址正确
- [ ] Office Viewer 可用
- [ ] Live Server 可用
- [ ] MarkItDown 可运行
- [ ] 知识库基础目录存在
- [ ] `.gitignore` 已配置
- [ ] Git 工作区状态正常
- [ ] 初始化提交已成功同步到 GitHub

如果某项失败，AI 继续排查，不要把失败项伪装成完成。

---

# 11. 初始化完成后归档本文件

全部验收通过后，AI：

1. 在本文件末尾追加“初始化结果”。
2. 写明日期、设备、GitHub 仓库、成功项和仍存在的问题。
3. 将本文件移动到：

```text
archive/system/初始化-YYYY-MM-DD.md
```

4. 提交并推送这次归档。

推荐提交信息：

```text
chore: archive completed initialization guide
```

从此以后，用户只需要在 TRAE 中告诉 AI 自己想做什么，不需要学习 Git 命令。

---

## 初始化结果

> 由执行初始化的 AI 在完成后填写。

- 日期：待填写
- 设备：待填写
- GitHub 仓库：待填写
- 状态：待初始化
- 备注：待填写
